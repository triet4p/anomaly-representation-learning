# Sprint 6 Task 6 — Execute geometry analysis remotely (corrected rerun)

- Ran the Task 5 execution copy `geometry_analysis.ipynb` against `.../server-geometry-run-1/geometry-cache` (cache-only; never loads model/dataset) with `V1_GEOMETRY_ANALYSIS=.../server-geometry-run-1/geometry-analysis`.
- Corrected rerun on source commit `a326e48`: same `uv run --no-sync jupyter nbconvert --execute` pattern; exit code 0 in 20.2 s wall (16:09:17→16:09:37 +07). Executed notebook has 4/4 code cells with outputs, zero errors. (First run on `69d665e`: exit 0 in 29.6 s; superseded.)
- Outputs: `metrics.json` 14,279 B; `neighbors.csv` 24,970,110 B (75,000 rows = 5000 queries × 15 neighbors, full score/label/fleet/regime columns); `figures/` **12 PNGs** (pca/tsne × anomaly_family, fleet, regime_summary, severity, split, plus uncolored base); `geometry-diagnostics.zip` ~13.2 MB. No runtime failure; no further rerun needed.
- Corrected readings: rank is **127/128 (near-full, not full)**; `normal_reference_distance` (mean 3.5745 over 5000 queries) is the mean distance to the nearest fitted normal references **within the extracted test cache with self-exclusion** — not a checkpoint-bank distance; `same_class_similarity` (count 4999, mean 0.4162) is a **capped within-cache** pair similarity under the per-class budget (different-class count 0 in this all-normal head sample, margin null); checkpoint-bank distance is reported separately from cache `S_pop` (mean 7.3446 ± 0.7053 over the 5000 rows), not from these metrics. All-normal limitation explicit (see task-7).
