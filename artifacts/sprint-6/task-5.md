# Sprint 6 Task 5 — Execute geometry extraction remotely

- Execution copies: `experiments/20260905/server-geometry-run-1/{geometry_extraction,geometry_analysis}.ipynb`, generated from commit `69d665e` canonicals with only first-cell paths set to concrete server paths (`SRC_DIR=/home/trietlm/.../src`, dataset `.../data/generated/production`, checkpoint `.../checkpoints/v1_representation_20260904_01.pt`, output `.../server-geometry-run-1/geometry-cache`). No autodetection anywhere.
- Command: `uv run --no-sync jupyter nbconvert --to notebook --execute ... --ExecutePreprocessor.timeout=-1` from the repo root; exit code 0 in 12.4 s wall (15:27:09→15:27:22 +07).
- Device: `cuda` (RTX 4060 Ti; manifest `extraction.device=cuda`). Task 18 device fix held — no index/device error across 157 bounded batches.
- Output: valid geometry cache — 5000 `test` records; `embeddings.npz` 2,415,218 B (5000×128 float32 + row_index + S_pred/S_pop), `records.csv` 734,034 B, `geometry-manifest.json` 2,378 B, `metrics.json`, `neighbors.csv` header; manifest `records_count` 5000, `splits` {"test": 5000}, per-file bytes+SHA-256 linkage verified in manifest; `fleet_precheck: "skipped: dataset manifest predates fleet metadata"` recorded; checkpoint provenance (step 7840, sha `7156f0…`, reference_count 5000 after the 5000-row bank cap).
- Failures fixed through the commit→push→pull loop (none hidden): (1) first attempt failed in 1.7 s with `NameError: Path` — the Task 2 conversion had dropped extraction cell-1 imports; fixed locally, committed `69d665e`, pulled, rebuilt copies, reran green. (2) Legacy pre-fleet production manifest would have tripped the fleet gate — fixed in `c9d726f` (recorded skip + per-batch guards) before the first real run.
- Head-5000 of production `test` is all normal (no anomaly families in this run); sampling `head`, seed 20260904.
