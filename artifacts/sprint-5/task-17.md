# Sprint 5 Task 17 — Pinned Kaggle Model checkpoint contract

## Exact Kaggle model handle/path contract

- Model page facts: owner/model `trietp1253201581/v1-representation-20260904-01`; framework PyTorch; variation `default`; version `1`; file `v1_representation_20260904_01.pt` (19.85 MB); no authored usage guide on the page.
- Mount/handle convention source: Kaggle kaggle-cli primary docs — `docs/models_metadata.md` (framework slugs include `pyTorch`; `${PATH}` = `/kaggle/input/<model_slug>/<framework>/<variation_slug>/<version>`; `${FILEPATH}` adds `/<filename>` for single-file bundles) and `docs/kernels_metadata.md` (`model_sources` five-part `<owner>/<model-slug>/<framework>/<variation-slug>/<version-number>`, e.g. `google/gemma/Transformers/gemma-2b/1`).
- One explicit identity used everywhere:
  - Handle: `trietp1253201581/v1-representation-20260904-01/pyTorch/default/1`
  - Expected mount: `/kaggle/input/v1-representation-20260904-01/pyTorch/default/1/v1_representation_20260904_01.pt`
- Resolver (`_resolve_checkpoint`, identical in every consumer): `V1_CHECKPOINT_PATH` → `V1_CHECKPOINT` → exact mount → `rglob('v1_representation_20260904_01.pt')` under `/kaggle/input` → local default `checkpoints/v1_representation.pt`. Checkpoint never bundled.

## Changed files

- `notebooks/infer_v1_representation.ipynb` (canonical): repaired known cell-1 defects (stray column-0 `load_checkpoint` import removed into the import block; added `import zipfile`; added `mad_threshold` to the inference import); added model-identity block; cell 2 keeps dataset detection verbatim but resolves the checkpoint via `_resolve_checkpoint()`; markdown checkpoint bullet now cites the model handle + override.
- `notebooks/kaggle/infer_v1_representation.ipynb` (**new**): byte-mirror of canonical (repo had no kaggle copy; follows train/geometry mirror pattern).
- `notebooks/geometry_extraction.ipynb` + `notebooks/kaggle/geometry_extraction.ipynb` (byte-identical): `CHECKPOINT_PATH = _resolve_checkpoint()` with model block; `V1_CHECKPOINT` override preserved; markdown cites the Kaggle Model.
- `notebooks/geometry_analysis.ipynb` (+ mirror): unchanged — cache-only, no checkpoint text stale (`load_checkpoint`/`torch.load`/`V1RepresentationModel`/`extract_embeddings` absent, verified).
- `exports/kaggle-20260905-01/`: root + `notebooks/kaggle/` mirrors of extraction/analysis/inference regenerated from canonicals with the `src.zip`→`/kaggle/working/src` bootstrap retained; `kernel-metadata.json` and `kernel-metadata-infer.json` gained `"model_sources": ["trietp1253201581/v1-representation-20260904-01/pyTorch/default/1"]` (analysis metadata intentionally model-free); `RUN_GUIDE.md` gained a "V1 checkpoint (Kaggle Model) contract" section (handle, UI attach path, `model_sources` push note, expected mount + filename fallback, local override) and updated steps 2/4/5; `export-manifest.json` inventory regenerated (64 entries) with model-aware description; `exports/kaggle-20260905-01.zip` regenerated.
- Untouched: historical exports 01–04, `notebooks/train_v1_representation.ipynb` (+kaggle copy), `src/*`, `scripts/*`, `tests/*`, Sprint 4; dataset identity (`anomaly-representation-20260903-01`, `V1_DATA_ROOT`) preserved separately.

## Checks (static only; no execution, inference, extraction, diagnostics, downloads)

- All code cells compile in 8 checkpoint consumers + 4 cache-only analyses; outputs cleared; cell ids unique.
- Contract tokens (`HANDLE`, filename, mount template, `_resolve_checkpoint`, `V1_CHECKPOINT_PATH`, `V1_MODEL_MOUNT`) present in all 8 consumers; forbidden checkpoint loaders absent from all 4 analyses.
- Parity: canonical==kaggle (extraction, inference-new, analysis); export root==mirror (all three).
- 18 packaged imports AST-resolve against bundled `src/`; zero `__pycache__`/`.pyc`/`.pyo` in export dir, `src.zip` (52 members), outer ZIP (65 entries); manifest matches disk; inner `src.zip` hash matches disk.
- Formatters, linters, project-wide suites skipped per assignment.

## Final artifacts

- `exports/kaggle-20260905-01/src.zip`: 107757 bytes (unchanged, still in sync), SHA-256 `232df891207694dd59f0fd6d900651300913c1a087efce34864e804f280eff28`.
- `exports/kaggle-20260905-01.zip`: 246217 bytes, SHA-256 `cbd96c3d53664a6db00a98cd567dcfbe97d98154f0d1bc0a4e1d41e305590de5`.

## Correction — review-generated bytecode purge (post-review)

- Cause: the Task 17 review imported from the export tree and created 46 `.pyc` files in 5 `src/**/__pycache__/` directories on disk (`representation/`, `representation/layers/`, `synth/`, `synth/anomalies/`, `synth/physics/`). The archives never contained them; only the on-disk export directory violated the zero-cache/bytecode rule.
- Fix: deleted all 5 `__pycache__` directories plus any stray `.pyc`/`.pyo` via filesystem removal only — no import from the export, no notebook execution, `PYTHONDONTWRITEBYTECODE=1` / `sys.dont_write_bytecode` during rechecks.
- Recheck (text/archive inspection only): export dir zero `__pycache__`/`.pyc`/`.pyo`; `src.zip` 52 members clean; outer ZIP 65 entries clean with inner `src.zip` hash equal to disk; manifest still equals tracked disk files (64 entries, sizes + SHA-256 all match) — no tracked content changed, so no manifest/src.zip/outer-ZIP regeneration.
- Final hashes (unchanged): `src.zip` 107757 bytes, SHA-256 `232df891207694dd59f0fd6d900651300913c1a087efce34864e804f280eff28`; outer ZIP 246217 bytes, SHA-256 `cbd96c3d53664a6db00a98cd567dcfbe97d98154f0d1bc0a4e1d41e305590de5`.
